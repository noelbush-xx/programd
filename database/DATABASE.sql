create table properties (
bot varchar(50), 
ip varchar(50), 
sessionid varchar(50), 
username varchar(50), 
userpassword varchar(50), 
loggedin varchar(50), 
property varchar(50), 
value varchar(50)
)

